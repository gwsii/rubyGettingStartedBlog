<h1>All Blog Posts</h1>

<%= link_to 'New article', new_article_path %>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @articles.each do |a| %>
    <tr>
      <td><%= a.title %></td>
      <td><%= a.text %></td>
    </tr>
  <% end %>
</table>
